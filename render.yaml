services:
  - type: web
    name: aegis-fraud-backend
    env: docker
    rootDir: fraud-backend
    plan: free
    envVars:
      - key: SPRING_DATA_MONGODB_URI
        sync: false # User must provide this in the Render dashboard
